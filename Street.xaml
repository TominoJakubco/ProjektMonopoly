<UserControl x:Class="WPF_monopoly.Street"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_monopoly"
             x:Name="StreetSpace"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid x:Name="SpaceGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!--<TextBlock x:Name="StreetNameText"
            Text="{Binding ElementName=StreetSpace, Path=Name}"
            Grid.Column="0"
            Grid.ColumnSpan="4"
            Grid.Row="3"
            FontSize="10pt"
            HorizontalAlignment="Center"
            VerticalAlignment="center"
        ></TextBlock>-->
    </Grid>
</UserControl>
